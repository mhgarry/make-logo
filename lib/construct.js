const fs = require('fs/promises');
const data = require('answers')
//we are prompting the user to create their svg file
	//we are creating a function to create the svg file container

await (async() => {
	 writeFile('\.svg', answers) = (this.shape = rect) {
		return `<svg height=300 width=200>
		<rect x=10 y=10 width=30 height=40 />`  || (this.shape = circle => () => `<svg height=300 width=200>
		<circle r=10 cx=100 cy=100 />`)|| (this.shape = triangle => () => `<svg height=300 width=200><polygon points= />`
		`<text x=80 y=40 font-size="30" text-anchor="middle" fill="${this.logoText}">Text</text>)
		<polyline transform="translate(140,30)" fill="transparent" stroke-width="1" stroke="${this.textColor}"
		  points"250,60 100,400 400,400"}"/>
		<path>
	  </svg>`}}
